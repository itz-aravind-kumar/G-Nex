apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-service
  namespace: gdrive
spec:
  replicas: 2
  selector:
    matchLabels:
      app: metadata-service
  template:
    metadata:
      labels:
        app: metadata-service
    spec:
      containers:
      - name: metadata-service
        image: gnexdrive/metadata-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8082
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "k8s"
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres:5432/gdrive_metadata
        - name: SPRING_DATASOURCE_USERNAME
          value: gdrive_user
        - name: SPRING_DATASOURCE_PASSWORD
          value: gdrive_pass
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: kafka:9092
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: metadata-service
  namespace: gdrive
spec:
  selector:
    app: metadata-service
  ports:
  - port: 8082
    targetPort: 8082
  type: ClusterIP
